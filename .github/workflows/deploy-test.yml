name: Deploy TEST

on:
  push:
    branches:
      - developer

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: echo "Deploying to TEST..."
        # TODO: connect to TEST server (SSH / Manus / other provider)
